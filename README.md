# Learning Playwright

This repository contains code and examples for learning Playwright automation.

## Project Structure

- `Chapter_01_Basics`: Basic Playwright concepts and examples.

## Setup

1.  Clone the repository:
    ```bash
    git clone https://github.com/iswaryabas-bit/LearningPlaywright.git
    ```
2.  Install dependencies:
    ```bash
    npm install
    ```

## Running Tests

To run the tests:

```bash
npx playwright test
```
